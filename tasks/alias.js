'use strict';

const gulp = require('gulp');

gulp.task('s', 'Alias for start.', ['start']);
gulp.task('a', 'Alias for assets.', ['assets']);
gulp.task('p', 'Alias for pre-publish.', ['prepublish']);
